import { RootState } from '@/store';
import { createSelector } from '@reduxjs/toolkit';

// const templateSelector = (state: RootState) => state.template;

// export const templateMsgSelector = createSelector(
//   templateSelector,
//   (template) => template.templateMsg
// );
